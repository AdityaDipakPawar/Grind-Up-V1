# Backend API Configuration (Vite)
# In development, the app will default to http://localhost:3000/api
# In production, the app will default to https://grind-up-v1.vercel.app/api
# You can override with VITE_API_URL below if needed.
VITE_API_URL=

# Development settings
VITE_ENV=development
